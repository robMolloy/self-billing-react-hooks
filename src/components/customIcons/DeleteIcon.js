import React from "react";
import MUIDeleteIcon from "@material-ui/icons/HighlightOff";

const DeleteIcon = (props) => {
  return <MUIDeleteIcon style={{ cursor: "pointer" }} {...props} />;
};

export default DeleteIcon;
